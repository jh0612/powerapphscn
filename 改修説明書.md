# 改修説明書

## 改修概要

`説明ファイル.md` の仕様に基づき、既存コードの不具合修正・未実装機能の追加を行った。

---

## 修正・追加内容一覧

### 1. `modConstants.bas` ― 定数追加

| 定数名 | 値 | 用途 |
|---|---|---|
| `COL_WEEKDAY_OT` | 10 | 集計結果配列：平日残業時間（合計）|
| `COL_WEEKEND_OT` | 11 | 集計結果配列：休日残業時間（合計）|
| `SET_CASE_NAME` | "CaseName" | Settings シートの案件名キー（明細書/発注書フォールバック用）|

---

### 2. `modUtils.bas` ― バグ修正

#### `GetWorkContentByDate` 日付解析バグ修正

**問題：** 作業期間が `yyyy/mm/dd-yyyy/mm/dd` 形式で保存されているにもかかわらず、`Split(dateParts(0), "/")(0)` で**年**を月として、`(1)` で**月**を日として読み込んでいた。

**修正：**
- `Split` 後の配列長を確認し、`yyyy/mm/dd` 形式（3要素）と `mm/dd` 形式（2要素）の両方に対応
- 正しく年・月・日を取得して `DateSerial` に渡す

---

### 3. `modMain.bas` ― 主要修正

#### 3-1. `ParseBeijingFormat` 全面書き直し

**問題（複数）：**
1. VBScript の正規表現で後読み（lookbehind）を使用していたが、VBScript は非対応のためエラー
2. `For r = 2 To lastRow` の外側ループと内側 `Do While dr = 2` が重複し、毎行で全データを再処理する無限的バグ
3. ファイル名から氏名を抽出できず処理が進まない
4. 平日/休日残業の区別なし（仕様で「現状未実装」と明記されていた機能）

**修正：**
1. FSO でベース名を取得後、`Split(fileName, "-")(2)` でインデックス2番目のセグメントを氏名として抽出
   - ファイル名形式：`考勤报表-北京幸福协力-氏名-XF-WZ019-20260501-20260531`
2. ループ構造を単純化：外側の `For r` を廃止し、D列日付が有効な行のみ1ループで収集
3. **平日/休日残業の区別を実装**：
   - `Weekday(dateVal) = vbSunday` or `vbSaturday` で判定
   - 休日：全勤務時間を `weekendOT`、標準工時 = 0
   - 平日：8h超過分を `weekdayOT`、標準工時 = min(勤務時間, 8h)
4. `dayInfo` 配列に `dayInfo(5) = weekdayOT`, `dayInfo(6) = weekendOT` を追加
5. 集計配列を `(0 To 11)` に拡張し `COL_WEEKDAY_OT` / `COL_WEEKEND_OT` に蓄積

#### 3-2. `ParseDalianFormat` / `ParseHenanFormat` 引数修正

**問題：** `Call ParseBeijingFormat(ws, priceDict)` と呼び出し時に `filePath` が渡されていなかった（引数不足）。

**修正：** `Call ParseBeijingFormat(filePath, ws, priceDict)` に修正。

#### 3-3. `GetCaseNameByWorkNo` 新規追加

`[name]` シートの L列（作業番号）を検索し、一致する M列（案件名）を返す。
見つからない場合は Settings の `CaseName`（`SET_CASE_NAME`）をフォールバックとして返す。

#### 3-4. `GenerateHSCN` 修正

| 修正箇所 | 変更前 | 変更後 |
|---|---|---|
| Timesheet A列（月）| 未書き込み | `dateM` を `dayInfo(0)` から算出して書き込み |
| Timesheet B列（日）| 未書き込み | `dateD` を `dayInfo(0)` から算出して書き込み |
| Timesheet C列（作業内容）| `newWS.Cells(8,1)` 等の未書き込み値を参照 | `dayInfo(0)` から正しく年・月・日を算出して `GetWorkContentByDate` に渡す |
| Timesheet I列（標準工時）| 休日を考慮せず一律に書き込み | 休日（`dayInfo(6) > 0`）は `stdH = 0` |
| AcceptanceRequest G列（数式）| `=Timesheet_人名!I5` | `=Timesheet_人名!I15`（仕様通りに修正）|
| AcceptanceRequest D列（開始日）| 未書き込み | 人員設定の作業期間、未設定は業務期間開始日 |
| AcceptanceRequest F列（終了日）| 未書き込み | 人員設定の作業期間、未設定は業務期間終了日 |
| AcceptanceRequest J列（数量）| 未書き込み | `1` |
| 残業行 | 未実装 | 平日/休日残業合計 > 0 の場合、次の行に追加：B=氏名, C=職級, D=開始日, F=終了日, H="1式", J=1, K=平日OT×15+休日OT×20 |

#### 3-5. `GenerateCompanyReport`（明細書）列マッピング修正

| 列 | 変更前 | 変更後（仕様通り）|
|---|---|---|
| A | 未書き込み | 人員作業期間（未設定は当月）|
| B | 作業内容 | 作業内容（未設定はプロジェクト設定の作業内容）|
| C | `"一式"` | 空白（仕様：空白）|
| D | 作業内容（誤り）| 案件名（`GetCaseNameByWorkNo` で取得）|
| E | 総工時（仕様外）| 空白（仕様未定義）|
| F | 氏名 ✓ | 氏名 ✓ |
| G | 単価 ✓ | 単価 ✓ |
| H | 未書き込み | 標準工時合計（`COL_TOTAL_HOURS`）|

#### 3-6. `GenerateOrderReport`（実績発注書）修正

| 修正箇所 | 変更前 | 変更後（仕様通り）|
|---|---|---|
| 開始行 | 14 | 13 |
| B列 | 氏名（誤り）| 案件名（`GetCaseNameByWorkNo`）|
| F列 | 人月（誤り）| 標準工時合計 |
| G列 | 職級（誤り）| 単価 |
| I列 | 未書き込み | `"要員"`（固定）|
| N列 | 未書き込み | 氏名 |
| O列 | 未書き込み | 作業番号 |

---

### 4. `frmProject.frm` ― 案件名フィールド追加

`UserForm_Initialize` と `btnSaveProject_Click` に案件名の保存・読み込みコードを追加。
`On Error Resume Next` で `txtCaseName` コントロールが存在しない場合も安全に動作する。

> **⚠️ 注意：** `txtCaseName` という名前の TextBox コントロールを VBA エディタで `frmProject` フォームに手動追加する必要があります。追加しない場合、案件名の UI 入力は不可（Settings シートの `CaseName` 行に直接入力することで代替可能）。

---

## 未解決の疑問点・確認事項

### Q1. Timesheet テンプレートの I15 セルについて

仕様書では `=Timesheet_人名!I15` を AcceptanceRequest の参照式として使用するよう指定されている。
コードは I15 を参照するよう修正済みだが、実際の `Timesheet_Template` ファイルにおいて **I15 が標準工時の合計セル（SUM 式）になっているか要確認**。
データは 8 行目から書き込まれるため、月次データ最大31行なら合計は 39 行付近になる可能性がある。テンプレートの構造を確認し、必要に応じて参照先（例：`I39` 等）を変更してください。

### Q2. 明細書 H2 の参照方式

仕様書では「対応 Timesheet 工作表 I15 標準工時」と記載されており、数式参照か値コピーか不明。
現状コードでは `COL_TOTAL_HOURS`（計算値）を直接書き込んでいる（別ファイルなので外部参照を避けた）。
数式参照が必要な場合は `[HSCNファイル名]Timesheet_人名!I15` 形式の外部参照式に変更してください。

### Q3. 発注書の出力形式

`GenerateOrderReport` の出力ファイル拡張子を `.xlsx` に変更（元は `.xls`）。テンプレートが `.xlsm` や `.xls` の場合は `outputFile` 末尾の拡張子を適宜変更してください。

### Q4. frmProject 案件名 UI

上記 **4.** に記載の通り、`txtCaseName` コントロールの手動追加が必要です。

---

## 変更ファイル一覧

| ファイル | 変更種別 |
|---|---|
| `代码/modConstants.bas` | 定数追加 |
| `代码/modUtils.bas` | バグ修正（日付解析）|
| `代码/modMain.bas` | バグ修正・機能追加（大規模）|
| `代码/frmProject.frm` | 案件名保存/読み込み追加 |
