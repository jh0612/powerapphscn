# 考勤汇总&单据生成工具说明文档
## 一、整体概述
本工具用于依托Excel数据源完成**考勤数据读取、工时核算、HSCN验收书/明细表/实际发注书批量生成**全流程作业；数据来源依赖固定Excel的`[name]`工作表，配置项分为主页面基础筛选、项目配置、人员配置、单价配置、日志查看五大模块，核心执行分为「考勤汇总计算」「多文件一键生成」两步。

## 二、主页面基础筛选区（据点/路径/人员/日期/文件列表）
### 1. 拠点選択（据点选择）
- 作用：区分考勤模板，**当前固定使用北京考勤模板**，后续可按需扩展多据点模板。
### 2. フォルダパス（文件夹路径）
- 选择考勤文件存放目录，选定后自动在`lstFiles`加载目录内全部文件。
### 3. 担当者名（负责人）
- 下拉数据源：`[name]`工作表A列人员名单；默认「すべて（全部）」展示全量文件，选定指定人员后，`lstFiles`仅筛选出该人员对应考勤文件。
### 4. 業務期間（业务周期）
- 格式：`yyyy/mm/dd~yyyy/mm/dd`，自带输入合法性校验，默认填充当月起止日期；
- 数据落地：起始日写入验收书`Acceptance Request`工作表`B11`，结束日写入`D11`。
### 5. lstFiles（文件列表）&全選択
- lstFiles：展示所选目录下考勤文件，受负责人筛选条件联动过滤；
- 全選択：一键勾选列表内全部文件。

## 三、プロジェクト設定（项目配置弹窗frmProject）
> 配置内容大部分回填至【HSCN験収書- Acceptance Request】指定单元格，新增待开发项：案件名录入框
|配置项|数据来源/填写规则|输出单元格位置|备注|
| ---- | ---- | ---- | ---- |
|Project name（项目名）|手动录入|B5|验收书|
|Supplier name（供应商名）|手动录入|B6|验收书|
|Service mode（业务形式）|下拉取自`[name]`I列|B7|验收书|
|作業内容（作业内容）|手动录入|B8|验收书|
|階段目標（阶段目标）|手动录入|B10|验收书|
|Contract No.（合同编号）|手动录入|J4|验收书|
|案件名|待新增录入项|-|无落地单元格，明细/发注书无匹配案件名时作为兜底取值|
|HSCN験収書模板路径|手动选择模板文件|-|支持后续修改文件名（未开发），模板命名示例：`HSCN验收书V1.82 Draft 2（再订正版）0511`|
|明細書模板路径|手动选择模板文件|-|支持后续修改文件名（未开发），模板命名示例：`明细（605）_大连金骋0511`|
|実績発注書模板路径|手动选择模板文件|-|支持后续修改文件名（未开发），模板命名示例：`【实际报价兼发注书】V2 （605）_大连金骋0511`|
|出力先|配置最终成品文件保存目录|-|单据生成落地路径|

- 底部按钮：保存、取消。

## 四、人員設定（人员配置弹窗btnPerson）
> 功能已完成落地，暂无需迭代；数据依托`[name]`工作表，配置项用于工时匹配与单据字段回填
1. 氏名：下拉取自`[name]`A列；会社名：取自`[name]`E列；
2. 作業期間：`yyyy/mm/dd-yyyy/mm/dd`格式，输入校验；作業内容、作業番号、レベル（职级）手动填写；
3. 操作按钮：追加、更新、消除、全消除、保存、取消。

## 五、単価表設定（单价配置btnPrice）
按钮保留预留，暂不开发原有逻辑；后续可改造为工时单价配置页面入口。

## 六、ログ一覧（日志btnLog）
异常报错展示面板，支持日志复制，功能已完成。

## 七、勤怠集計実行（考勤汇总运算btnExecute·核心计算）
### 前置条件
已勾选lstFiles文件、项目配置+人员配置全部完成，才可执行汇总。
### 1. 考勤文件命名规则
示例：`考勤报表-北京幸福协力-周潔栄-XF-WZ019-20260501-20260531`，文件名包含人员姓名用于数据匹配。
### 2. 考勤取值列
读取Excel：D列日期、E列上午上班时间、G列下午下班时间。
### 3. 工时换算规则
1. 时间取整：上班分钟≥30进至下半小时，＜30舍去；下班同理（例：9:28→9:30、18:50→18:30）；
2. 基准工时：标准在岗8h（9:30~18:30，午休固定1h），超出部分计加班，缺打卡计缺勤，工时上限8h；
3. 加班区分：需标记**工作日加班/周末加班**（当前功能缺失，其余计算逻辑已实现），用于后续加班单价核算。
### 4. 单价匹配
根据人员姓名读取单价表，获取对应人员计价单价。
### 5. 输出结果
完成单人全周期工时、缺勤、工作日/周末加班时长汇总。

## 八、ファイル一括生成（单据批量导出btnGenerate·最终出稿）
### 前置条件
考勤汇总执行成功后按钮才可启用；按「HSCN验收书→明细書→実績発注書」顺序循环多人员生成单据。
### 8.1 HSCN験収書（验收书生成）
1. 新建工作表：以`Timesheet_人名`命名，基于`Timesheet_Template`模板复制生成；
    - 固定单元格写入：C3=公司名、C4=姓名、E4=职级、D5=业务起始日；
    - 第8行起循环写入考勤数据：A=月份、B=日期，仅存在考勤记录的日期才生成行；C=人员配置作业内容（按姓名+作业周期匹配）、E=上班时间、F=下班时间、G=加班时长、H=缺勤时长、I=标准工时（上限8h）。
2. 回填`Acceptance Request`工作表（15行起循环）：
    - 正常出勤行：B=姓名、C=职级、D=开始日、F=结束日、G=引用公式`=Timesheet_人名!I15`、I=人员单价、J=1；
    - 存在加班时新增下一行：B=姓名、C=职级、D=开始日、F=结束日、G空、H=1式、I空、J=1、K=工作日加班×15+周末加班×20；

### 8.2 明細書（明细表生成，第2行起循环）
|单元格|填充规则|
| ---- | ---- |
|A2|人员作业周期（未配置则默认当月）|
|B2|人员作业内容（未配置则取项目配置作业内容）|
|C2|空白|
|D2|人员作业番号匹配`[name]`L列，匹配成功取M列案件名；无匹配取项目配置案件名|
|F2|人员姓名|
|G2|人员计价单价|
|H2|对应Timesheet工作表I15标准工时|

### 8.3 実績発注書（实际发注书，13行起循环）
|单元格|填充规则|
| ---- | ---- |
|B13|作业番号匹配`[name]`L列，匹配成功取M列案件名；无匹配取项目配置案件名|
|F13|Timesheet工作表I15标准工时|
|G13|人员计价单价|
|I13|固定填写：要員|
|N13|人员姓名|
|O13|人员配置作业番号|